# No build-time secrets required for GitHub sync.
# Users paste a personal access token in the app (Settings → GitHub Sync).
#
# Create a classic token with the "gist" scope:
# https://github.com/settings/tokens/new?scopes=gist&description=Sniplet
